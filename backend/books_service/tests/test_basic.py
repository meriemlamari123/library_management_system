"""
Test factice pour que pytest passe
À remplacer par de vrais tests plus tard
"""

# ✅ Import not needed for now since it's a simple test
# This is just a placeholder test to make sure CI/CD runs correctly

def test_basic():
    """Test basique pour que le pipeline passe"""
    # 🧮 Vérifie une simple addition
    # Si ce test passe, le pipeline est correctement configuré
    assert 1 + 1 == 2
